# voetlab — feature manifest

Auto-generated by `voetlab/docs/build_docs.py`. One row per feature file.

| Folder | File | Summary |
|---|---|---|
| core | `fixtures.py` | Footage-driven test harness + artifact dumping. |
| core | `provenance.py` | Provenance — trace every event back to the frame(s) it was captured from. |
| core | `result.py` | Result — the universal success indicator for every voetlab feature. |
| detection | `detect.py` | P1 — per-frame detection (persons + ball) via YOLO. |
| tracking | `ball_tracker.py` | P3 — ball trajectory (linear interpolation). |
| tracking | `ball_trajectory.py` | T6 — Kalman constant-velocity ball trajectory (upgrade over linear interp). |
| tracking | `player_tracker.py` | P2 — player tracking (ByteTrack) over detection boxes. |
| tracking | `role_filter.py` | P5/T3 — referee/goalkeeper role filter (pure bbox heuristics). |
| tracking | `team_classifier.py` | P4 — team classification (KMeans k=2 on median HSV torso color). |
| tracking | `tracker_factory.py` | T9 — tracker factory: ByteTrack (supervision) or BoT-SORT/OC-SORT (ultralytics model.track). |
| calibration | `features.py` | Calibration feature — TVCalib solver (primary, accurate) → H; DLT fallback. |
| calibration | `homography.py` | T4 — pitch homography pure engine: broadcast pixels → real-world meters. |
| calibration | `homography_lines.py` | DLT homography from LINE correspondences (SoccerNet/sn-calibration baseline). |
| calibration | `keypoint.py` | T4 (detector half) — pitch landmark keypoints → homography point pairs. |
| calibration | `metric.py` | T4/T5/T11/T12 bridge — convert pixel tracks to metric-space via a homography. |
| calibration | `tvcalib_solver.py` | TVCalib full camera solver → homography (the ACCURATE calibration path). |
| events | `events.py` | P5 — events: possession, passes, tackles, interceptions — every event frame-provenanced. |
| stats | `stats.py` | P6 — physical + aggregate stats. |
| tactics | `features.py` | T11/T12 feature wrappers — read pixel tracks + meta H, convert to metric, compute tactics. |
| tactics | `pitch_control.py` | T11 — Spearman pitch-control surface (probability team A controls each pitch point). |
| tactics | `voronoi.py` | T12 — Voronoi / dominant-region territory (metric pitch). |
| reliability | `features.py` | Reliability feature — per-stat confidence from ball/track quality signals. |
| reliability | `reliability.py` | T10 — reliability signal: an honest per-stat confidence from measurable CV-quality signals. |
| viz | `annotate.py` | Annotated-video renderer — burns voetlab analysis onto each source frame. |
| viz | `charts.py` | T13 — mplsoccer chart adapters (dashboard chart engine). |
| pipeline | `cli.py` | CLI: ``python -m voetlab.pipeline.cli <video> [--feature NAME] [--max-frames N]``. |
| pipeline | `default.py` | P7 — default pipeline wiring + single-feature isolation. |
| pipeline | `registry.py` | Feature registry. |
| pipeline | `runner.py` | Pipeline runner: dependency-order execution with shared state, failure flagging, |
