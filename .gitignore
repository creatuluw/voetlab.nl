# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Test / build artifacts
.pytest_cache/
.mypy_cache/
tests/out/

# Editable install metadata
*.egg-link

# Agent / session state (pi harness)
.pi/

# OS / editor
.DS_Store
.idea/
.vscode/

# Consolidated model weights — distributed via FTP, not git (large binaries).
# Only models/README.md is tracked; the .pt files are FTP assets.
/models/*.pt

# Oversized vendored asset — exceeds GitHub's 100 MB file limit.
# Calibration checkpoint referenced by meta["calib_checkpoint"]; download separately
# (see docs/BUILD_FROM_SCRATCH.md Phase 14).
voetlab/external/tvcalib/data/
